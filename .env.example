# Travel utilities (change: add-travel-services) — 复制为 .env.dev 并填入真实值；
# .env.dev 已被 .gitignore 忽略，禁止提交（backend/README.md 的 ./run.sh dev 注入方式）。
# 留空 = 哨兵：不装配 OpenWeatherMap 客户端，天气字段返回 null（优雅降级）。
OPENWEATHER_API_KEY=
# AI 助手 chat + embedding（change: ai-rag 切供）：DashScope（compatible-mode / qwen-plus /
# text-embedding-v3）。留空 = 哨兵：ChatClient 不装配，对话端点 503 AI_UNAVAILABLE（fail-closed）。
DASHSCOPE_API_KEY=
# AI 助手 RAG 向量库（change: ai-rag-milvus）：Milvus Standalone gRPC 端点。
# 默认值已在 application.yml 承载（localhost:19530，docker compose up -d 三件套），一般无需覆盖。
MILVUS_HOST=localhost
MILVUS_PORT=19530
